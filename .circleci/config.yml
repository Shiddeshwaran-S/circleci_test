version: 2.1
  build:
    docker:
      - image: cimg/node:14.10.1
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run test
          command: npm test
